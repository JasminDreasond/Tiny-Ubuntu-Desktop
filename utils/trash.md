### Emptying Trash via CLI

For a good terminal way to manage the trash bin, use the `trash-cli` utility:

1. **Install the package:**
   ```bash
   sudo apt install trash-cli
   ```

2. **Empty all trash cans:**
   ```bash
   trash-empty
   ```

*Note: This tool automatically handles trash directories across all mounted drives and system locations.*
